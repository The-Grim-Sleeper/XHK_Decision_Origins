
military_service_none = {
	potential = {
		OR = {
			from = { is_hive_empire = no }
			NOT = { has_trait = trait_hive_mind }
		}
	}

	allow = {
		hidden_trigger = { exists = from }
		custom_tooltip = {
			fail_text = same_species_disallowed
			NOT = { is_same_species = from }
		}
		custom_tooltip = {
			fail_text = species_has_military_slavery
			NOT = {
				has_slavery_type = { country = from type = slavery_military }
			}
		}
		custom_tooltip = {
			fail_text = MACHINE_SPECIES_CANNOT_MACHINE
			if = {
				limit = {
					from = {
						is_machine_empire = yes
						NOR = {
							has_country_flag = decision_shroud_forged
							has_origin = origin_shroud_forged
						}
					}
				}
				OR = {
					NOR = {
						has_trait = trait_mechanical
						has_trait = trait_machine_unit
					}
					has_trait = trait_robot_suppressed
				}
			}
		}
		custom_tooltip = {
			fail_text = MECHANICAL_SPECIES_CANNOT_MACHINE
			if = {
				limit = {
					from = {
						has_authority = auth_machine_intelligence
						OR = {
							has_country_flag = decision_shroud_forged
							has_origin = origin_shroud_forged
						}
					}
				}
				NOT = { has_trait = trait_machine_unit }
			}
		}
	}
	ai_will_do = {
		factor = 0
	}
}
